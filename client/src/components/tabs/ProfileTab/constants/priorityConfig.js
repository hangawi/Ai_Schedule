/**
 * ===================================================================================================
 * priorityConfig.js - 우선순위 설정 상수
 * ===================================================================================================
 *
 * 📍 위치: 프론트엔드 > client/src/components/tabs/ProfileTab/constants
 *
 * 🎯 주요 기능:
 *    - 시간표 우선순위별 색상 클래스 정의 (1-3단계)
 *    - 우선순위별 라벨 텍스트 제공
 *
 * 🔗 연결된 파일:
 *    - ../components/*.js - 시간표 셀 렌더링 시 우선순위 색상 적용
 *    - ../index.js (ProfileTab) - 우선순위 선택 UI에서 라벨 표시
 *
 * 💡 UI 위치:
 *    - 탭: 프로필 탭 (ProfileTab)
 *    - 섹션: 시간표, 달력
 *    - 경로: 앱 실행 > 프로필 탭 > 시간 선택 시 우선순위 색상 표시
 *
 * ✏️ 수정 가이드:
 *    - 이 파일을 수정하면: 우선순위 색상 및 라벨이 변경됨
 *    - 색상 변경: PRIORITY_COLORS의 Tailwind 클래스 수정
 *    - 라벨 변경: PRIORITY_LABELS의 텍스트 수정
 *    - 우선순위 레벨 추가: 새로운 키 추가 (예: 4: '최우선')
 *
 * 📝 참고사항:
 *    - 우선순위 1: 조정 가능 (연한 파란색)
 *    - 우선순위 2: 보통 (중간 파란색)
 *    - 우선순위 3: 선호 (진한 파란색)
 *    - Tailwind CSS 클래스 사용 (bg-, text-, border-)
 *
 * ===================================================================================================
 */

// 우선순위 설정 상수

/**
 * PRIORITY_COLORS - 우선순위별 색상 클래스
 *
 * @description 시간표 셀에 적용할 우선순위별 Tailwind CSS 클래스
 * @type {Object.<number, string>}
 * @constant
 *
 * @property {string} 1 - 조정 가능 (연한 파란색)
 * @property {string} 2 - 보통 (중간 파란색)
 * @property {string} 3 - 선호 (진한 파란색)
 *
 * @example
 * const priorityClass = PRIORITY_COLORS[priority];
 * <div className={priorityClass}>시간 슬롯</div>
 *
 * @note
 * - 숫자가 높을수록 진한 색상 (우선순위 높음)
 * - bg-: 배경색, text-: 텍스트색, border-: 테두리색
 * - Tailwind CSS 클래스 문자열 형태
 */
// 우선순위별 색상 클래스
export const PRIORITY_COLORS = {
  1: 'bg-blue-200 text-blue-800 border-blue-300',
  2: 'bg-blue-400 text-white border-blue-500',
  3: 'bg-blue-600 text-white border-blue-700'
};

/**
 * PRIORITY_LABELS - 우선순위별 라벨
 *
 * @description 사용자에게 표시할 우선순위 라벨 텍스트
 * @type {Object.<number, string>}
 * @constant
 *
 * @property {string} 1 - '조정 가능' (유연한 시간)
 * @property {string} 2 - '보통' (일반 선호)
 * @property {string} 3 - '선호' (강한 선호)
 *
 * @example
 * const label = PRIORITY_LABELS[priority];
 * <button>{label}</button>
 *
 * @note
 * - 우선순위 선택 버튼이나 범례에 표시
 * - 1: 가장 유연함, 3: 가장 선호함
 */
// 우선순위별 라벨
export const PRIORITY_LABELS = {
  1: '조정 가능',
  2: '보통',
  3: '선호'
};

/**
 * ===================================================================================================
 * LoadingState.js - 로딩 상태 표시 컴포넌트
 * ===================================================================================================
 *
 * 📍 위치: 프론트엔드 > client/src/components/tabs/ProfileTab/components
 *
 * 🎯 주요 기능:
 *    - 프로필 탭에서 데이터 로딩 중일 때 로딩 메시지 표시
 *    - 간단한 텍스트로 로딩 상태 UI 렌더링
 *
 * 🔗 연결된 파일:
 *    - ../index.js (ProfileTab) - 로딩 상태일 때 이 컴포넌트 렌더링
 *    - ../hooks/useProfileData.js - 데이터 로드 중에 로딩 상태 전달
 *
 * 💡 UI 위치:
 *    - 탭: 프로필 탭 (ProfileTab)
 *    - 섹션: 전체 화면 (데이터 로딩 중)
 *    - 경로: 앱 실행 > 프로필 탭 > 초기 로드 또는 새로고침 시 표시
 *
 * ✏️ 수정 가이드:
 *    - 이 파일을 수정하면: 로딩 상태 UI가 변경됨
 *    - 로딩 스타일 변경: div의 className 추가/수정
 *    - 로딩 메시지 변경: "로딩 중..." 텍스트 수정
 *    - 스피너 추가: <div>에 spinner 아이콘 또는 애니메이션 추가
 *
 * 📝 참고사항:
 *    - 매우 간단한 로딩 표시 컴포넌트 (재사용 가능)
 *    - props 없이 동작하는 순수 표시 컴포넌트
 *    - 스피너나 애니메이션 없이 텍스트만 표시
 *
 * ===================================================================================================
 */

// 로딩 상태 컴포넌트

import React from 'react';

/**
 * LoadingState - 로딩 상태 표시 컴포넌트
 *
 * @description 데이터 로드 중일 때 로딩 메시지를 표시하는 UI 컴포넌트
 * @returns {JSX.Element} 로딩 상태 UI
 *
 * @example
 * {isLoading && <LoadingState />}
 *
 * @note
 * - props 없이 동작하는 간단한 컴포넌트
 * - "로딩 중..." 메시지만 표시
 * - 필요 시 스피너 추가 가능
 */
export const LoadingState = () => {
  return <div>로딩 중...</div>;
};

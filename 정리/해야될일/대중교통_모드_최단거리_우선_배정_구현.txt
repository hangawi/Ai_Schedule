================================================================================
대중교통 모드 - 최단거리 기반 차례대로 배정 알고리즘 구현 계획
================================================================================

📅 작성일: 2025-12-09
🎯 목표: 한 학생의 수업 종료 후 가장 가까운 학생에게 이동하되, 이동시간 + 전체 수업시간이
        선호시간 내에 들어가는 경우에만 배정하는 로직 구현

================================================================================
1. 요구사항 분석
================================================================================

[현재 상황]
- 대중교통 모드에서 A, B, C 학생이 모두 월/화/수 13~17시 선호시간 보유
- A의 수업: 이동 20분 → 13:20 시작 → 14:00 종료

[핵심 요구사항]
1. A 수업 종료(14:00) 후 다음 학생 선택
   → A 위치 기준 가장 가까운 학생 우선 선택
   → B(1시간 30분) vs C(1시간 50분) → B 선택

2. 시간 충족 조건 (모두 만족해야 배정)
   ✓ 조건 1: 이동시간이 선호시간 범위 내
   ✓ 조건 2: 도착 후 할당된 수업시간 전체가 선호시간 내 확보

3. 조건 불충족 시 처리
   - 예시: B 선호시간이 16:00까지만 가능
   - A 종료(14:00) + 이동(1.5h) = 도착 15:30
   - 남은 시간: 30분 (수업시간 부족!)
   - 결과: 월요일 스킵 → 화요일로 배정

[제약사항]
- ⚠️ 협의(negotiation) 로직과 혼동 금지
- 예외시간 침범 금지
- 기존 모든 알고리즘 로직 보존 필수
- 모든 파일과 호환되어야 함

================================================================================
2. 현재 알고리즘 파일 구조
================================================================================

schedulingAlgorithm/
├── index.js                              # 메인 진입점
├── constants/
│   ├── priorityConstants.js             # 우선순위 상수
│   ├── errorMessages.js                 # 에러 메시지
│   ├── schedulingConstants.js           # 스케줄링 상수
│   └── timeConstants.js                 # 시간 관련 상수
├── utils/
│   ├── dateUtils.js                     # 날짜 유틸리티
│   ├── arrayUtils.js                    # 배열 유틸리티
│   ├── timeUtils.js                     # ⭐ 시간 계산 유틸리티
│   └── slotUtils.js                     # 슬롯 유틸리티
├── validators/
│   ├── inputValidator.js                # 입력 검증
│   ├── scheduleValidator.js             # 스케줄 검증
│   └── conflictValidator.js             # 충돌 검증
├── helpers/
│   ├── conflictMerger.js                # 충돌 병합
│   ├── carryOverHelper.js               # 이월 처리
│   ├── memberHelper.js                  # 멤버 관련 헬퍼
│   ├── assignmentHelper.js              # ⭐ 배정 헬퍼
│   └── timetableHelper.js               # 시간표 헬퍼
└── services/
    ├── conflictIdentificationService.js # 충돌 식별
    ├── conflictResolutionService.js     # 충돌 해결
    ├── scoringService.js                # 점수 계산
    ├── multiWeekSchedulingService.js    # 다중 주 스케줄링
    ├── timetableCreationService.js      # ⭐ 시간표 생성
    └── slotAssignmentService.js         # ⭐ 슬롯 배정 (핵심!)

================================================================================
3. 구현 계획
================================================================================

[Phase 1] 분석 단계
─────────────────────────────────────────────────────────────────
□ slotAssignmentService.js 현재 로직 분석
  - 현재 어떻게 다음 학생을 선택하는지 파악
  - 대중교통 모드 관련 로직 위치 확인
  - 시간 계산 방식 확인

□ timeUtils.js 시간 계산 로직 분석
  - 이동시간 계산 함수 확인
  - 시간 충족 여부 판단 로직 확인

□ assignmentHelper.js 배정 로직 분석
  - 배정 헬퍼에서 어떤 역할을 하는지 파악
  - 거리 계산 관련 코드 확인

[Phase 2] 설계 단계
─────────────────────────────────────────────────────────────────
□ 새로운 함수 설계
  1. findNearestStudentWithSufficientTime()
     - 입력: 현재 학생 위치, 종료 시간, 후보 학생 목록
     - 출력: 가장 가까우면서 시간이 충족되는 학생
     - 로직:
       a. 각 후보 학생까지의 이동시간 계산
       b. 도착 시간 + 수업시간이 선호시간 내인지 확인
       c. 조건 만족하는 학생 중 가장 가까운 학생 선택
       d. 없으면 null 반환 (다음 날로 이월)

  2. validateTimeSlotWithTravel()
     - 입력: 시작시간, 이동시간, 수업시간, 선호시간 범위
     - 출력: boolean (충족 여부)
     - 로직:
       a. 도착시간 = 시작시간 + 이동시간
       b. 종료시간 = 도착시간 + 수업시간
       c. 도착시간 >= 선호시작 && 종료시간 <= 선호종료

  3. sortStudentsByDistance()
     - 입력: 현재 위치, 학생 목록
     - 출력: 거리 순으로 정렬된 학생 목록
     - 로직: Google Maps Distance Matrix API 결과 기반 정렬

□ 기존 함수 수정 지점 확인
  - slotAssignmentService에서 다음 학생 선택 부분
  - 대중교통 모드 조건 분기 추가

[Phase 3] 구현 단계
─────────────────────────────────────────────────────────────────
□ Step 1: utils/timeUtils.js에 헬퍼 함수 추가
  - validateTimeSlotWithTravel() 구현
  - 기존 함수와 충돌 없도록 주의

□ Step 2: helpers/assignmentHelper.js에 거리 정렬 함수 추가
  - sortStudentsByDistance() 구현
  - findNearestStudentWithSufficientTime() 구현

□ Step 3: services/slotAssignmentService.js 수정
  - 대중교통 모드 조건 분기 추가
  - 기존 로직 보존하면서 새 로직 통합
  - 조건:
    if (transportMode === 'public') {
      // 새로운 최단거리 우선 로직
      const nextStudent = findNearestStudentWithSufficientTime(...);
      if (!nextStudent) {
        // 다음 날로 이월
      }
    } else {
      // 기존 로직 유지
    }

□ Step 4: 예외시간 체크 통합
  - 기존 예외시간 검증 로직 재사용
  - 새로운 시간 슬롯에 대해서도 예외시간 체크

[Phase 4] 테스트 단계
─────────────────────────────────────────────────────────────────
□ 단위 테스트 시나리오
  1. A → B (조건 만족) 배정 성공
  2. A → B (시간 부족) → C 시도
  3. A → B, C 모두 시간 부족 → 다음 날 이월
  4. 예외시간 침범 시 스킵
  5. 다중 학생 순차 배정 테스트

□ 통합 테스트
  - 기존 알고리즘과의 호환성 확인
  - 다른 모드(도보, 자동차)는 영향 없는지 확인
  - 협의 로직과 혼동 없는지 확인

[Phase 5] 문서화
─────────────────────────────────────────────────────────────────
□ 코드 주석 작성
  - 각 함수에 JSDoc 형식 주석 추가
  - 알고리즘 로직 설명 추가

□ 변경사항 기록
  - 어떤 파일을 수정했는지
  - 왜 수정했는지
  - 기존 로직과의 차이점

================================================================================
4. 핵심 알고리즘 의사코드
================================================================================

```javascript
// 대중교통 모드 다음 학생 선택 알고리즘

function assignNextStudentInPublicTransportMode(
  currentStudent,      // 현재 수업 종료한 학생
  currentEndTime,      // 현재 수업 종료 시간
  candidateStudents,   // 아직 배정 안 된 학생 목록
  currentDay,          // 현재 요일
  currentLocation      // 현재 위치 (수업 종료 위치)
) {
  // 1. 후보 학생들을 거리 순으로 정렬
  const sortedCandidates = sortStudentsByDistance(currentLocation, candidateStudents);

  // 2. 각 후보에 대해 순차적으로 검증
  for (const candidate of sortedCandidates) {
    // 2-1. 이동시간 계산
    const travelTime = calculateTravelTime(currentLocation, candidate.address, 'public');

    // 2-2. 도착시간 계산
    const arrivalTime = currentEndTime + travelTime;

    // 2-3. 후보 학생의 선호시간 가져오기
    const preferenceTime = candidate.preferenceTime[currentDay];

    // 2-4. 할당된 수업시간 (예: 1시간)
    const classDuration = candidate.allocatedDuration;

    // 2-5. 시간 충족 여부 확인
    const isTimeSufficient = validateTimeSlotWithTravel(
      arrivalTime,
      classDuration,
      preferenceTime.start,
      preferenceTime.end
    );

    // 2-6. 예외시간 침범 확인
    const hasNoConflict = !checkExceptionTimeConflict(
      candidate,
      arrivalTime,
      arrivalTime + classDuration
    );

    // 2-7. 조건 모두 만족하면 배정
    if (isTimeSufficient && hasNoConflict) {
      return {
        student: candidate,
        startTime: arrivalTime,
        endTime: arrivalTime + classDuration,
        travelTime: travelTime
      };
    }
  }

  // 3. 모든 후보가 조건 불충족 → 다음 날로 이월
  return {
    carryOver: true,
    nextDay: getNextDay(currentDay)
  };
}

function validateTimeSlotWithTravel(arrivalTime, classDuration, prefStart, prefEnd) {
  const classEndTime = arrivalTime + classDuration;

  // 도착시간이 선호시간 시작 이후이고
  // 수업 종료시간이 선호시간 종료 이전이어야 함
  return arrivalTime >= prefStart && classEndTime <= prefEnd;
}
```

================================================================================
5. 주의사항 및 체크리스트
================================================================================

[⚠️ 반드시 지켜야 할 사항]
□ 기존 알고리즘 로직 절대 변경 금지
  - 조건 분기(if문)로 대중교통 모드만 분리
  - 다른 모드는 기존 로직 그대로 유지

□ 협의(negotiation) 로직과 분리
  - 이것은 자동 배정 알고리즘
  - 협의는 별도 프로세스

□ 예외시간 체크 필수
  - 식사시간, 수면시간 등 개인시간 침범 금지
  - 기존 예외시간 검증 함수 재사용

□ 시간 계산 정확성
  - 이동시간 + 수업시간 모두 고려
  - 분 단위 정확한 계산

[🔍 테스트 체크리스트]
□ 시나리오 1: 정상 케이스
  - A(14:00 종료) → B(1.5h 이동, 15:30 도착, 16:30 종료)
  - B 선호시간이 17:00까지 → ✅ 배정 성공

□ 시나리오 2: 시간 부족 케이스
  - A(14:00 종료) → B(1.5h 이동, 15:30 도착)
  - B 선호시간이 16:00까지 → 30분만 가능 → ❌ 스킵
  - C로 시도 또는 다음 날 이월

□ 시나리오 3: 예외시간 충돌
  - A(14:00 종료) → B(1.5h 이동, 15:30 도착)
  - B의 16:00~17:00 개인시간(식사) → ❌ 스킵

□ 시나리오 4: 다중 학생 순차 배정
  - A → B → C → D 순차적으로 가까운 순서로 배정
  - 각 단계에서 시간 충족 검증

================================================================================
6. 예상 파일 수정 목록
================================================================================

[생성할 파일]
없음 (기존 구조 활용)

[수정할 파일]
1. utils/timeUtils.js
   - validateTimeSlotWithTravel() 추가
   - 약 20줄 추가 예상

2. helpers/assignmentHelper.js
   - sortStudentsByDistance() 추가
   - findNearestStudentWithSufficientTime() 추가
   - 약 50줄 추가 예상

3. services/slotAssignmentService.js
   - 대중교통 모드 조건 분기 추가
   - 기존 로직 내 if문 추가 (약 30줄)

4. constants/schedulingConstants.js (필요 시)
   - 대중교통 모드 관련 상수 추가

[읽기만 할 파일]
- index.js (진입점 확인)
- validators/* (예외시간 검증 로직 재사용)
- services/conflictResolutionService.js (충돌 처리 방식 참고)

================================================================================
7. 구현 순서
================================================================================

1️⃣ 기존 코드 분석 (30분)
   - slotAssignmentService.js 읽기
   - timeUtils.js 읽기
   - assignmentHelper.js 읽기

2️⃣ 헬퍼 함수 구현 (1시간)
   - validateTimeSlotWithTravel() 구현 및 테스트
   - sortStudentsByDistance() 구현 및 테스트

3️⃣ 메인 로직 구현 (1시간)
   - findNearestStudentWithSufficientTime() 구현
   - slotAssignmentService에 통합

4️⃣ 테스트 및 디버깅 (1시간)
   - 각 시나리오별 테스트
   - 버그 수정

5️⃣ 문서화 및 정리 (30분)
   - 주석 추가
   - 변경사항 기록

총 예상 시간: 약 4시간

================================================================================
8. 성공 기준
================================================================================

✅ 한 학생 수업 종료 후 가장 가까운 학생 순으로 배정 시도
✅ 이동시간 + 수업시간이 선호시간 내에 모두 들어가는 경우만 배정
✅ 조건 불충족 시 다음 날로 자동 이월
✅ 예외시간 침범 시 해당 학생 스킵
✅ 기존 알고리즘과 충돌 없음
✅ 다른 모드(도보, 자동차)는 영향 없음
✅ 협의 로직과 혼동 없음

================================================================================
9. 참고사항
================================================================================

[대중교통 모드 관련 기존 코드 위치]
- Google Maps Distance Matrix API 호출 부분 찾기
- transportMode 파라미터 전달 부분 확인
- 기존 이동시간 계산 로직 재사용

[이월(carryOver) 처리]
- carryOverHelper.js 활용
- 다음 날짜로 이월 시 선호시간 재확인

[디버깅 팁]
- console.log로 각 단계별 거리, 시간 출력
- 어떤 학생이 왜 스킵되었는지 로그 남기기
- 최종 배정 결과 검증

================================================================================
END OF PLAN
================================================================================

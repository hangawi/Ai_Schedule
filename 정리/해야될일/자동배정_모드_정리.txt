================================================================================
자동 배정 모드 종합 정리
================================================================================
작성일: 2025-12-01

이 문서는 다음 파일들의 내용을 통합한 것입니다:
- 3.txt
- assignment_mode_requirements.txt

================================================================================


================================================================================
원본 파일: 3.txt
================================================================================

# 자동 배정 & 일정 수정 모드 요구사항

## 1) 자동배정 기능 확장

### 현재 문제
- “자동배정” 기능이 오직 이번 주 범위에만 적용되고 있음.

### 개선 요구사항
- 자동배정 실행 시 **현재 월 전체 범위**(예: 11월 1일 ~ 11월 30일)에 대해 자동 배정이 수행되도록 변경한다.


## 2) 일정 수정 모드: 주간 / 월간 모드 분리

### A. 주간 모드(Weekly Mode)
- 주간 모드에서는 **이번 주 내에서만** 일정 이동 및 수정이 가능해야 한다.
- 다른 주로의 이동은 제한되며, 경고 또는 오류 메시지를 제공해야 한다.
- 예시:
  - “이번주 수요일 일정을 이번주 금요일로 옮겨줘” → 허용
  - “이번주 수요일 일정을 다음주 화요일로 옮겨줘” → 불가

### B. 월간 모드(Monthly Mode)
- 월간 모드에서는 **월 전체 날짜 기준으로 일정 이동 및 수정이 가능**해야 한다.
- 사용자의 요청은 **주차 기반** 또는 **날짜 기반** 모두 허용된다.

#### 1) 주차 기반 이동 예시
- “11월 둘째 주 월요일 일정을 넷째 주 수요일로 옮겨줘”
  - 11월 2주차 월요일 → 날짜 계산 후 매핑 (예: 11월 11일)
  - 11월 4주차 수요일 → 날짜 계산 후 매핑 (예: 11월 27일)
  - 계산된 날짜 기준으로 일정 이동 수행

#### 2) 날짜 기반 이동 예시
- “11월 1일 일정을 11월 15일로 옮겨줘”
  - 단순 날짜 간 이동 수행

#### 3) 월 범위 외 이동 시 처리
- 이동 결과가 해당 월을 벗어날 경우 경고 또는 확인 절차 필요  
  (예: 11월 30일 → 12월 3일)


## 4) 전체 요약
- **자동배정**: 이번주만 → 해당 월 전체로 확장  
- **주간 모드**: 이번 주 내 이동만 가능  
- **월간 모드**: 월 전체 날짜/주차 기반 이동 가능  



================================================================================
원본 파일: assignment_mode_requirements.txt
================================================================================

# 자동 배정 모드 요구사항

## 1. 보통 모드 (Normal Mode)

### 개념
- 모든 멤버가 **동시에 공평하게 시작**한다고 가정
- 방에 들어온 순서는 고려하지 않음
- 같은 시간대에 여러 멤버가 가능하면 **공평하게 기회 부여**

### 배정 우선순위
1. **우선순위(priority)** 높은 멤버 우선
2. 우선순위가 같으면:
   - 가능한 시간이 **적은 멤버** 우선 (최소 유연성 원칙)
   - 또는 다른 선택지가 적은 멤버 우선

### 예시
- 월 13-17에 a, b 모두 가능
- a 우선순위 2, b 우선순위 2 (같음)
- a는 가능한 시간 20슬롯, b는 22슬롯
- a가 먼저 배정됨 (가능한 시간이 적으므로)

---

## 2. 선착순 모드 (First-Come-First-Served Mode)

### 개념
- 방에 **들어온 순서**대로 우선권 부여
- 먼저 들어온 멤버가 시간대를 먼저 선택할 권리
- 단, **우선순위(priority)가 더 높으면 순서 무시**

### 배정 우선순위
1. **우선순위(priority)** 높은 멤버 최우선
2. 우선순위가 같으면:
   - **방 입장 순서** 빠른 멤버 우선
   - 먼저 들어온 멤버가 시간대 선택

### 예시 1: 우선순위가 다른 경우
- a가 먼저 입장, 월요일 우선순위 2
- b가 나중 입장, 월요일 우선순위 3
- **b가 먼저 배정됨** (우선순위 3 > 2)

### 예시 2: 우선순위가 같은 경우
- a가 먼저 입장, 월요일 우선순위 2
- b가 나중 입장, 월요일 우선순위 2
- **a가 먼저 배정됨** (먼저 입장했으므로)

---

## 3. 현재 문제

### 현재 상황
- 멤버 순서: a, b, c, d, e, h, f, g (입장 순서)
- 목 13-17에 f, g, h 모두 가능
- 타임테이블 생성 시 배열 순서: **h → f → g**
- 1단계에서 **h가 먼저 처리됨** → h가 목 13-17 배정
- f는 목 13-17 못 가져감 + 수 13-17도 d가 가져감 → **f 배정 실패**

### 문제점
- 현재는 의도하지 않은 순서로 배정됨
- 보통 모드도 선착순 모드도 아닌 상태
- members 배열 순서에 따라 결과가 달라짐

---

## 4. 구현 방향

### 보통 모드
- availableMembers를 정렬:
  1. priority 내림차순
  2. 전체 가능 슬롯 수 오름차순 (적은 멤버 우선)

### 선착순 모드
- availableMembers를 정렬:
  1. priority 내림차순
  2. 방 입장 순서 (createdAt 또는 joinedAt)

### UI 추가
- 방장 설정에서 "배정 모드" 선택 옵션 추가
  - [ ] 보통 모드 (공평 배정)
  - [ ] 선착순 모드 (입장 순서 우선)
  - [ ] 연속 배정 모드 (같은 시간대 연속 배치)

---

## 5. 연속 배정 모드 (Consecutive Assignment Mode)

### 개념
- 같은 시간대에 가능한 멤버들을 **연속으로 배정**
- 예: 월요일 9-12시에 a, b, c가 모두 가능하면
  - a: 월 9:00-10:00
  - b: 월 10:00-11:00
  - c: 월 11:00-12:00
- 시간대가 겹치는 멤버들을 한 곳에 모아서 배정

### 배정 우선순위
1. **우선순위(priority)** 높은 멤버 최우선
2. 우선순위가 같으면:
   - **보통 모드**: 가능한 시간 적은 멤버 우선
   - **선착순 모드**: 방 입장 순서 우선
3. 같은 날짜/시간대에 가능한 멤버들을 **연속된 1시간 블록**으로 배정

### 예시 1: 기본 연속 배정
**상황:**
- 월 9-12 (6슬롯): a, b, c 모두 가능
- 월 13-17 (8슬롯): d, e 가능
- 모두 우선순위 3, 필요 시간 3시간 (6슬롯)

**결과:**
- a: 월 09:00-10:00 (2슬롯)
- b: 월 10:00-11:00 (2슬롯)
- c: 월 11:00-12:00 (2슬롯)
- d: 월 13:00-14:00 (2슬롯)
- e: 월 14:00-15:00 (2슬롯)

### 예시 2: 우선순위 고려
**상황:**
- 월 9-12 (6슬롯): a(우선순위 3), b(우선순위 2), c(우선순위 3)
- 필요 시간 각 3시간 (6슬롯)

**결과:**
- a: 월 09:00-10:00 (2슬롯) ← 우선순위 3
- c: 월 10:00-11:00 (2슬롯) ← 우선순위 3
- b: 월 11:00-12:00 (2슬롯) ← 우선순위 2

### 장점
- 관리 편의성: 같은 시간대 멤버들이 연속으로 배치되어 일정 관리가 쉬움
- 시간대 그룹화: 오전/오후 등으로 멤버들을 자연스럽게 그룹화
- 효율적 활용: 큰 시간 블록을 여러 멤버가 나눠서 효율적으로 사용

### 단점
- 선호 시간 무시: 개별 멤버의 세부 시간 선호도를 고려하지 않음
- 분산 불가: 한 멤버가 여러 날에 분산 배정받기 어려움

### 구현 방법
1. 시간대별로 가능한 멤버들을 그룹화
2. 각 그룹 내에서 멤버들을 정렬 (우선순위 → 보통/선착순 기준)
3. 정렬된 순서대로 연속된 1시간 블록씩 배정
4. 한 멤버가 필요 시간을 다 채우면 다음 멤버로 이동

---

## 6. 구현 현황 (2025-11-27)

### ✅ 완료된 작업

#### 1. Room 모델 스키마 업데이트
**파일:** `Ai_Schedule/server/models/room.js`

**변경 사항:**
```javascript
settings: {
  // ❌ 제거: ownerPreferences (방장 선호시간)

  // ✅ 추가: assignmentMode (배정 모드)
  assignmentMode: {
    type: String,
    enum: ['normal', 'first_come_first_served', 'consecutive'],
    default: 'normal'
  }
}
```

#### 2. RoomCreationModal UI 업데이트
**파일:** `Ai_Schedule/client/src/components/modals/RoomCreationModal.js`

**변경 사항:**
- settings 상태에 assignmentMode 추가 (기본값: 'normal')
- 라디오 버튼 UI 추가:
  - ⭕ 보통 모드 (기본값)
  - ⚪ 선착순 모드
  - ⚪ 연속 배정 모드

### 🔄 진행 중인 작업

#### 3. RoomManagementModal UI 업데이트
**파일:** `Ai_Schedule/client/src/components/modals/RoomManagementModal.js`

**TODO:**
- formData에 assignmentMode 추가
- 기존 방 수정 시 assignmentMode 표시/변경 가능하도록 UI 추가
- RoomCreationModal과 동일한 라디오 버튼 형태로 구현

### 📝 미완료 작업

#### 4. 선착순 모드 알고리즘 구현
**파일:** `Ai_Schedule/server/services/schedulingAlgorithm/services/slotAssignmentService.js`

**구현 위치:** Stage 2 멤버 정렬 로직

**현재 코드 (보통 모드):**
```javascript
// 우선순위 높은 멤버 먼저, 같으면 가용 슬롯 적은 멤버 먼저
const remainingMembers = Object.keys(memberAvailableSlots)
  .sort((a, b) => {
    const priorityDiff = memberMaxPriority[b] - memberMaxPriority[a];
    if (priorityDiff !== 0) return priorityDiff;
    return memberAvailableSlots[a] - memberAvailableSlots[b];
  });
```

**변경 필요:**
```javascript
// assignmentMode에 따라 정렬 기준 변경
const remainingMembers = Object.keys(memberAvailableSlots)
  .sort((a, b) => {
    const priorityDiff = memberMaxPriority[b] - memberMaxPriority[a];
    if (priorityDiff !== 0) return priorityDiff;

    if (assignmentMode === 'first_come_first_served') {
      // 선착순: joinedAt 비교
      const memberA = members.find(m => extractMemberId(m) === a);
      const memberB = members.find(m => extractMemberId(m) === b);
      return new Date(memberA.joinedAt) - new Date(memberB.joinedAt);
    } else {
      // 보통 모드: 가용 슬롯 적은 순
      return memberAvailableSlots[a] - memberAvailableSlots[b];
    }
  });
```

**필요한 데이터:**
- members 배열에서 joinedAt 정보 추출
- assignmentMode를 함수 파라미터로 전달받아야 함

#### 5. 연속 배정 모드 알고리즘 구현
**파일:** `Ai_Schedule/server/services/schedulingAlgorithm/services/slotAssignmentService.js`

**새로운 함수 필요:** `assignByConsecutiveBlocks`

**알고리즘:**
```javascript
const assignByConsecutiveBlocks = (timetable, assignments, memberRequiredSlots, ownerId, members, assignmentMode) => {
  // 1. 날짜별 시간 블록 그룹화
  const dateTimeBlocks = {}; // { '2025-11-24': { '09:00-12:00': [슬롯키들], '13:00-17:00': [...] } }

  // 2. 각 블록별로 가능한 멤버 추출 및 정렬
  Object.keys(dateTimeBlocks).forEach(date => {
    Object.keys(dateTimeBlocks[date]).forEach(timeBlock => {
      const slots = dateTimeBlocks[date][timeBlock];

      // 이 시간 블록에서 가능한 멤버들
      const availableMembers = getAvailableMembersForBlock(slots);

      // 정렬: 우선순위 → (보통모드 or 선착순)
      const sortedMembers = sortMembers(availableMembers, assignmentMode);

      // 3. 연속으로 1시간씩 배정
      let currentSlotIndex = 0;
      for (const member of sortedMembers) {
        const needed = memberRequiredSlots[member] - assignments[member].assignedHours;
        if (needed <= 0) continue;

        // 1시간(2슬롯) 배정
        const toAssign = Math.min(2, needed, slots.length - currentSlotIndex);
        for (let i = 0; i < toAssign; i++) {
          assignSlot(timetable, assignments, slots[currentSlotIndex + i], member);
        }
        currentSlotIndex += toAssign;

        if (currentSlotIndex >= slots.length) break;
      }
    });
  });
};
```

**호출 위치:**
```javascript
// index.js의 runAutoSchedule 함수 내
if (roomSettings.assignmentMode === 'consecutive') {
  assignByConsecutiveBlocks(timetable, assignments, memberRequiredSlots, ownerId, members, roomSettings.assignmentMode);
} else {
  assignByTimeOrder(timetable, assignments, memberRequiredSlots, ownerId, members, roomSettings.assignmentMode);
}
```

#### 6. 백엔드 API 수정
**파일:** 자동 배정 관련 컨트롤러/서비스

**TODO:**
- assignmentMode를 roomSettings에서 추출하여 알고리즘에 전달
- 기존 ownerPreferences 관련 코드 제거 또는 주석 처리

#### 7. 기타 UI 업데이트
**TODO:**
- 배정 모드 설명 툴팁 추가 (각 모드별 상세 설명)
- 배정 결과 화면에 현재 사용된 모드 표시
- 배정 모드 변경 시 경고 메시지 (기존 배정에 영향)

---

## 7. 테스트 시나리오

### 보통 모드 테스트
- [ ] 8명 (a-h) 동일 우선순위, 가용 시간 다름 → 가용 시간 적은 순서로 배정 확인
- [ ] h (가용 시간 적음)가 먼저 목요일 배정받는지 확인

### 선착순 모드 테스트
- [ ] a(먼저 입장), b(나중 입장), 동일 우선순위 → a가 먼저 배정
- [ ] a(먼저 입장, 우선순위 2), b(나중 입장, 우선순위 3) → b가 먼저 배정

### 연속 배정 모드 테스트
- [ ] 월 9-12시에 a, b, c 가능 → a(9-10), b(10-11), c(11-12) 형태로 배정
- [ ] 우선순위 다른 경우 높은 우선순위부터 연속 배정

---

## 8. 다음 작업 순서

1. ✅ Room 모델 업데이트 (완료)
2. ✅ RoomCreationModal UI (완료)
3. 🔄 RoomManagementModal UI
4. ⏳ 선착순 모드 알고리즘
5. ⏳ 연속 배정 모드 알고리즘
6. ⏳ 백엔드 API 연결
7. ⏳ 테스트 및 디버깅



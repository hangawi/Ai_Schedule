================================================================================
이동시간 금지시간 침범 문제 - 작업 기록 및 해결 계획
================================================================================
작성일: 2025-12-18
상태: 진행 중 - 추가 문제 발견

================================================================================
📋 작업 요약
================================================================================

[완료된 작업]
1. 서버 측 자동배정 로직 수정 (대중교통 모드)
   - index.js:272 버그 수정 (roomSettings.blockedTimes로 변경)
   - timeUtils.js validateTimeSlotWithTravel 함수 수정
   - 이동시간 + 수업시간을 하나의 블록으로 검증하도록 변경

2. 클라이언트 측 이동시간 계산 로직 수정 (보기 버튼)
   - travelScheduleCalculator.js 수정
   - 금지시간 회피 로직: 금지시간 **이전**으로 조정 → 금지시간 **이후**로 조정
   - 17-24시 절대 금지시간 추가 (하드코딩)
   - 17시 이후 배치 불가 처리 추가

[발견된 새로운 문제]
- B 학생이 아예 사라짐 (배정이 안됨)
- 이동시간 때문에 조정된 시간이 선호시간을 벗어나면 배정 자체가 취소됨

================================================================================
🔍 문제 원인 분석
================================================================================

[핵심 문제]
현재 시스템의 로직:
1. 선호시간 = 배정 가능한 시간
2. 선호시간 외 시간 = 자동으로 금지시간 취급
3. 별도 지정한 금지시간(예: 점심시간 12-13시)만 명시적 금지시간
4. 17-24시 "절대 금지시간"은 실제로 존재하지 않음
   - 단지 선호시간이 아니라서 배정 불가능한 것

[문제 시나리오]
예시) A 학생
- 선호시간: 09:00-12:00, 13:00-17:00
- 원본 배정: 09:00-12:00 수업 (3시간)
- 이동시간: 70분 필요

보기 버튼 클릭 시:
1. 이동시간 계산: 09:00-10:10 (70분)
2. 수업시간 조정: 10:10-13:10
3. 금지시간(12:00-13:00) 감지
4. 금지시간 이후로 조정: 13:00-14:10 이동 + 14:10-17:10 수업
5. ✅ 성공 (선호시간 13:00-17:00 내에 있음)

예시) B 학생
- 선호시간: 13:00-16:00
- 원본 배정: 13:00-16:00 수업 (3시간)
- 이전 학생 종료: 17:10
- 이동시간: 10분 필요

보기 버튼 클릭 시:
1. 이전 학생이 17:10에 끝남
2. 이동시간: 17:10-17:20 (10분)
3. 수업시간 조정: 17:20-20:20
4. 17시 이후 배치 불가 처리 발동
5. ❌ 실패 (canPlace = false)
6. 원본 슬롯 유지 시도 → 하지만 B가 아예 사라짐

[근본 원인]
- 이동시간을 고려하면 선호시간을 벗어나는 경우가 많음
- 현재 로직은 선호시간을 벗어나면 배치 불가능으로 처리
- "보기 버튼"은 이미 배정된 스케줄에 이동시간을 **시각화**하는 기능
  → 배정을 취소하면 안 됨!

================================================================================
🎯 해결 방안
================================================================================

[방안 1: 보기 버튼 로직 수정 (권장)]
목표: 이동시간 시각화는 하되, 배정 자체는 유지

수정 파일: client/src/services/travelScheduleCalculator.js

핵심 변경:
1. 금지시간 체크 로직 제거 또는 완화
   - 명시적 금지시간(점심시간 등)만 체크
   - 17-24시 절대 금지시간 제거
   - 선호시간 체크 제거

2. 시간 조정 로직 변경
   - 이전 활동 종료 시간을 기준으로 이동시간 배치
   - 선호시간을 벗어나도 배치 허용
   - 단, UI에서 "선호시간 외" 표시 추가

3. canPlace 로직 제거
   - 무조건 이동시간 반영
   - 시간이 겹치면 순차적으로 배치

구현:
```javascript
// 금지시간 체크를 명시적 금지시간만으로 제한
const explicitBlockedTimes = currentRoom.settings?.blockedTimes || [];
// 17-24시 절대 금지시간 제거

// 이전 활동 종료 시간 기준으로 배치
let newTravelStartMinutes = Math.max(slotStartMinutes, previousActivityEndMinutes);

// 명시적 금지시간과 겹치면 금지시간 이후로 조정
for (const blocked of explicitBlockedTimes) {
    // ... 조정 로직
}

// canPlace 체크 제거 - 무조건 배치
```

[방안 2: 자동배정 로직 수정 (근본적 해결)]
목표: 자동배정 시 이동시간을 미리 고려

수정 파일: server/services/schedulingAlgorithm/*

핵심 변경:
1. 일반 모드 자동배정 시에도 이동시간 고려 옵션 추가
2. 선호시간 외 시간도 배정 가능하도록 옵션 추가
3. 배정 우선순위:
   - 1순위: 선호시간 내 + 이동시간 포함
   - 2순위: 선호시간 외 + 이동시간 포함
   - 3순위: 선호시간 내 + 이동시간 제외

주의: 대규모 수정 필요, 테스트 시간 많이 소요

[방안 3: 하이브리드 (단기 해결책)]
목표: 최소한의 수정으로 동작하도록

수정 내용:
1. 보기 버튼에서 17-24시 절대 금지시간 제거
2. 명시적 금지시간(점심시간 등)만 체크
3. 선호시간 체크는 유지하되, 벗어나면 경고 표시만
4. canPlace는 명시적 금지시간 침범 시에만 false

================================================================================
✅ 권장 작업 순서
================================================================================

[Phase 1: 긴급 수정 (오늘)]
1. travelScheduleCalculator.js 수정
   - 17-24시 절대 금지시간 제거
   - canPlace 로직 완화 (명시적 금지시간만 체크)
   - 테스트: B 학생이 정상적으로 표시되는지 확인

2. 선호시간 외 표시 추가 (선택사항)
   - UI에서 선호시간 외 시간대를 다른 색상으로 표시
   - 예: 선호시간 내 = 녹색, 선호시간 외 = 주황색

[Phase 2: 중장기 개선 (추후)]
1. 자동배정 로직에 이동시간 고려 추가
2. 선호시간 외 배정 옵션 추가
3. 배정 우선순위 시스템 구현

================================================================================
📝 상세 구현 코드
================================================================================

[수정 1: 17-24시 절대 금지시간 제거]
파일: client/src/services/travelScheduleCalculator.js
위치: 381-390번 줄

변경 전:
```javascript
const blockedTimes = currentRoom.settings?.blockedTimes || [];

// ✅ 17-24시 절대 금지시간 추가 (하드코딩)
const absoluteBlockedTime = {
    name: '17-24시 절대 금지시간',
    startTime: '17:00',
    endTime: '24:00'
};
const allBlockedTimes = [...blockedTimes, absoluteBlockedTime];
```

변경 후:
```javascript
// 명시적으로 지정한 금지시간만 사용 (점심시간 등)
const allBlockedTimes = currentRoom.settings?.blockedTimes || [];
```

[수정 2: 17시 이후 배치 불가 로직 제거]
파일: client/src/services/travelScheduleCalculator.js
위치: 420-427번 줄

변경 전:
```javascript
// ✅ 17시 절대 금지시간 체크: 조정 후에도 17시를 넘으면 배치 불가
if (adjustedStartTime >= 17 * 60 || adjustedEndTime > 17 * 60) {
    console.warn(`⚠️ [배치 불가] 17시 이후로는 배정 불가능`);
    console.warn(`   조정된 시간: ${this.formatTime(adjustedStartTime)}-${this.formatTime(adjustedEndTime)}`);
    console.warn(`   → 이동시간 없이 원본 슬롯 유지`);
    canPlace = false;
    break;
}
```

변경 후:
```javascript
// 17시 이후 체크 제거 - 선호시간 외에도 배치 허용
// (단, 명시적 금지시간은 여전히 체크)
```

[수정 3: canPlace 로직 간소화 (선택사항)]
현재 canPlace는 금지시간 침범 시에만 false
→ 명시적 금지시간(점심시간 등)만 체크하도록 이미 변경됨
→ 추가 수정 불필요

================================================================================
🧪 테스트 시나리오
================================================================================

[테스트 1: 기본 케이스]
- A 학생: 09:00-12:00 수업, 이동시간 70분
- 예상: 13:00-14:10 이동 + 14:10-17:10 수업 (점심시간 회피)

[테스트 2: 선호시간 외 배치]
- B 학생: 13:00-16:00 수업, 이전 종료 17:10, 이동시간 10분
- 예상: 17:10-17:20 이동 + 17:20-20:20 수업 (선호시간 외에도 배치)

[테스트 3: 명시적 금지시간 회피]
- C 학생: 11:00-14:00 수업, 점심시간 12:00-13:00
- 예상: 금지시간 이후로 조정

================================================================================
⚠️ 주의사항
================================================================================

1. 선호시간 개념의 변경
   - 기존: 선호시간 = 유일한 배정 가능 시간
   - 변경: 선호시간 = 선호하는 시간 (권장 시간)
   - 선호시간 외에도 배정 가능 (단, 명시적 금지시간 제외)

2. UI 업데이트 필요
   - 선호시간 외 시간대를 시각적으로 구분
   - 사용자에게 "선호시간 외" 안내 메시지 추가

3. 성능 고려
   - 이동시간 계산은 Google Maps API 호출
   - 과도한 호출 방지 위해 캐싱 활용

4. 백엔드와의 일관성
   - 보기 버튼(프론트엔드)과 자동배정(백엔드)의 로직 차이
   - 추후 통합 필요

================================================================================
📌 다음 작업
================================================================================

1. ✅ 긴급: travelScheduleCalculator.js 수정 (17-24시 제거, canPlace 완화)
2. ⏳ 테스트: B 학생 정상 표시 확인
3. ⏳ UI 개선: 선호시간 외 시간대 구분 표시
4. 📅 추후: 자동배정 로직 개선 (이동시간 고려)

================================================================================
 [금지시간 체크] blockedTimes: [{…}]0: endTime: "13:00"name: "점심시간"startTime: "12:00"_id: "69392f09a4f5f9d5b0467c7c"[[Prototype]]: Objectlength: 1[[Prototype]]: Array(0)
travelScheduleCalculator.js:396 🚫 [금지시간 감지] 점심시간 (12:00-13:00)
travelScheduleCalculator.js:397    현재 이동: 09:00-10:10
travelScheduleCalculator.js:398    현재 수업: 10:10-13:10
travelScheduleCalculator.js:406    필요한 총 시간: 250분 (이동 70분 + 수업 180분)
travelScheduleCalculator.js:407    조정된 시작 시간: 13:00 (금지시간 13:00 이후로 시작)
travelScheduleCalculator.js:415 ✅ [금지시간 회피 성공] 조정 완료:
travelScheduleCalculator.js:416    이동: 13:00-14:10
travelScheduleCalculator.js:417    수업: 14:10-17:10
travelScheduleCalculator.js:418    시작 시간(13:00) >= 금지시간 종료(13:00) ✅
travelScheduleCalculator.js:472 📊 [travelSlots 추가] {날짜: '2025-12-03T00:00:00.000Z', from: '방장', to: '삼삼 삼', startTime: '13:00', endTime: '14:10', …}
travelScheduleCalculator.js:490 ✅ [조정된 슬롯 추가]: {원본: '09:00-12:00', 이동: '13:00-14:10', 조정된수업: '14:10-17:10', 추가개수: 25, 이전종료: '00:00', …}
travelScheduleCalculator.js:295 🔄 [슬롯 처리 중]: {날짜: '2025-12-03', 시간: '13:00-16:00', 사용자: '이이', 과목: '자동 배정'}
travelScheduleCalculator.js:341 🚗 [이동시간 계산] {from: '삼삼 삼', to: '이이 이', travelDurationSeconds: 399, travelDurationMinutes: 10, 원래값_초: 399, …}
travelScheduleCalculator.js:370 ✅ [이동시간 슬롯 차지] {출발지: '삼삼 삼', 도착지: '이이 이', 원래_수업: '13:00-16:00', 이동시간: '17:10-17:20 (10분)', 조정된_수업: '17:20-20:20', …}
travelScheduleCalculator.js:385 🔍 [금지시간 체크] blockedTimes: [{…}]
travelScheduleCalculator.js:472 📊 [travelSlots 추가] {날짜: '2025-12-03T00:00:00.000Z', from: '삼삼 삼', to: '이이 이', startTime: '17:10', endTime: '17:20', …}
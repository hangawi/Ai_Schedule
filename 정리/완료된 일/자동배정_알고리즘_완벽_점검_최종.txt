================================================================================
                   자동배정 알고리즘 완벽 점검 보고서
================================================================================
작성일: 2025-12-09
프로젝트: Ai_Schedule (MeetAgent)
점검 범위: 서버/클라이언트 전체 자동배정 시스템
================================================================================

목차
====
1. 전체 시스템 아키텍처
2. 서버 측 완전 조사
3. 클라이언트 측 조사
4. 데이터 흐름 추적
5. 외부 의존성
6. 숨겨진 로직 및 비활성화된 기능
7. 에러 처리
8. 성능 및 최적화
9. 환경 변수
10. 누락된 부분 및 개선 제안

================================================================================
1. 전체 시스템 아키텍처
================================================================================

[자동배정 시스템 아키텍처]

┌─────────────────────────────────────────────────────────────────┐
│                        클라이언트 (React)                          │
├─────────────────────────────────────────────────────────────────┤
│  - CoordinationTab (조정 탭)                                      │
│  - AutoSchedulerPanel (자동배정 패널)                             │
│  - AutoConfirmBanner (자동확정 배너)                              │
│  - TravelModeButtons (이동수단 선택)                              │
│  - ConfirmScheduleModal (확정 모달)                               │
└─────────────────────────────────────────────────────────────────┘
                            ↓ HTTP API
┌─────────────────────────────────────────────────────────────────┐
│                    API 라우터 (Express)                           │
├─────────────────────────────────────────────────────────────────┤
│  POST /api/coordination/rooms/:roomId/run-schedule               │
│  POST /api/coordination/rooms/:roomId/confirm-schedule           │
│  DELETE /api/coordination/rooms/:roomId/time-slots               │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                   컨트롤러 레이어                                  │
├─────────────────────────────────────────────────────────────────┤
│  - coordinationSchedulingController.js                           │
│    ↳ runAutoSchedule()     // 자동배정 실행                      │
│    ↳ confirmSchedule()     // 수동 확정                          │
│    ↳ deleteAllTimeSlots()  // 전체 슬롯 삭제                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│              스케줄링 알고리즘 (Core Engine)                       │
├─────────────────────────────────────────────────────────────────┤
│  schedulingAlgorithm/index.js (메인 클래스)                       │
│    ↳ runAutoSchedule()                                           │
│                                                                   │
│  [서비스 레이어]                                                  │
│  - timetableCreationService.js      // 타임테이블 생성            │
│  - conflictIdentificationService.js // 충돌 식별                 │
│  - slotAssignmentService.js         // 슬롯 배정                 │
│  - publicTransportAssignmentService.js  // 대중교통 모드          │
│  - multiWeekSchedulingService.js    // 다중 주 배정               │
│                                                                   │
│  [헬퍼 레이어]                                                    │
│  - assignmentHelper.js    // 배정 로직, 이동시간 계산             │
│  - memberHelper.js        // 멤버 처리                            │
│  - timetableHelper.js     // 타임테이블 조작                      │
│  - carryOverHelper.js     // 이월시간 처리                        │
│  - conflictMerger.js      // 충돌 병합                            │
│                                                                   │
│  [검증 레이어]                                                    │
│  - inputValidator.js      // 입력 검증                            │
│  - scheduleValidator.js   // 스케줄 검증                          │
│  - conflictValidator.js   // 충돌 검증                            │
│                                                                   │
│  [유틸리티 레이어]                                                │
│  - dateUtils.js           // 날짜 처리                            │
│  - timeUtils.js           // 시간 처리                            │
│  - slotUtils.js           // 슬롯 처리                            │
│  - arrayUtils.js          // 배열 처리                            │
│                                                                   │
│  [상수]                                                           │
│  - schedulingConstants.js // 배정 상수                            │
│  - timeConstants.js       // 시간 상수                            │
│  - priorityConstants.js   // 우선순위 상수                        │
│  - errorMessages.js       // 에러 메시지                          │
│                                                                   │
│  총 파일 수: 24개                                                 │
│  총 코드 라인: 4,517줄                                            │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                    데이터베이스 (MongoDB)                          │
├─────────────────────────────────────────────────────────────────┤
│  - Room (방 정보, timeSlots, settings)                            │
│  - User (defaultSchedule, scheduleExceptions, personalTimes)     │
│  - ActivityLog (활동 로그)                                        │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                    크론잡 (자동확정)                               │
├─────────────────────────────────────────────────────────────────┤
│  jobs/autoConfirmSchedule.js                                     │
│  - 매 1분마다 실행                                                │
│  - autoConfirmAt 시간 체크                                        │
│  - 자동으로 confirmSchedule() 실행                                │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                  외부 API (Google)                                │
├─────────────────────────────────────────────────────────────────┤
│  - Google Maps Distance Matrix API (이동시간 계산)                │
│  - Gemini API (AI 분석, 충돌 해결 제안)                           │
└─────────────────────────────────────────────────────────────────┘

================================================================================
2. 서버 측 완전 조사
================================================================================

2.1 스케줄링 알고리즘 디렉토리 구조
---------------------------------
server/services/schedulingAlgorithm/
├── index.js (325줄) - 메인 클래스
├── constants/
│   ├── priorityConstants.js
│   ├── errorMessages.js
│   ├── schedulingConstants.js
│   └── timeConstants.js
├── utils/
│   ├── dateUtils.js
│   ├── timeUtils.js
│   ├── slotUtils.js
│   └── arrayUtils.js
├── validators/
│   ├── inputValidator.js
│   ├── scheduleValidator.js
│   └── conflictValidator.js
├── helpers/
│   ├── assignmentHelper.js (378줄) - 배정 핵심 로직
│   ├── memberHelper.js
│   ├── timetableHelper.js
│   ├── conflictMerger.js
│   └── carryOverHelper.js
└── services/
    ├── timetableCreationService.js (340줄) - 타임테이블 생성
    ├── conflictIdentificationService.js
    ├── slotAssignmentService.js (612줄) - 슬롯 배정 전략
    ├── conflictResolutionService.js
    ├── publicTransportAssignmentService.js (221줄) - 대중교통 모드
    ├── multiWeekSchedulingService.js (90줄) - 다중 주
    └── scoringService.js

총 파일: 24개
총 코드 라인: 약 4,517줄 (주석 제외)

2.2 컨트롤러
-----------
[coordinationSchedulingController.js - 1,024줄]
주요 함수:
1. runAutoSchedule(req, res)
   - 위치: 라인 9-393
   - 역할: 자동배정 API 엔드포인트
   - 파라미터:
     * minHoursPerWeek (기본값: 3)
     * numWeeks (기본값: 4)
     * currentWeek (시작 주)
     * assignmentMode ('normal', 'first_come_first_served', 'from_today')
   - 처리 순서:
     1) 방 및 멤버 조회 (populate)
     2) 권한 확인 (방장만 가능)
     3) 기존 자동배정 슬롯 삭제 (협의 슬롯 보존)
     4) 입력 검증 (선호시간표 필수)
     5) schedulingAlgorithm.runAutoSchedule() 호출
     6) 결과를 room.timeSlots에 저장
     7) carryOver 처리
     8) autoConfirmAt 설정 (1분 후 - 실험용)
     9) 활동 로그 기록
     10) Socket.io 이벤트 전송

2. deleteAllTimeSlots(req, res)
   - 위치: 라인 395-536
   - 역할: 모든 타임슬롯 삭제 + 선호시간 복구
   - 처리:
     * timeSlots 배열 비우기
     * autoConfirmAt 해제
     * 멤버들의 personalTimes에서 해당 방 관련 항목 삭제
     * deletedPreferencesByRoom에서 백업된 선호시간 복구

3. confirmSchedule(req, res)
   - 위치: 라인 538-1024
   - 역할: 자동배정된 시간을 개인일정으로 확정
   - 처리:
     * 자동배정 슬롯 필터링 (assignedBy 있는 것)
     * 연속된 슬롯 병합 (30분 단위 → 1시간 이상 블록)
     * 각 조원의 personalTimes에 추가
     * defaultSchedule에서 배정된 부분 제거 (분할 유지)
     * deletedPreferencesByRoom에 백업
     * 방장의 personalTimes에 모든 조원 일정 추가
     * autoConfirmAt 해제
     * VersionError 재시도 로직 (최대 3회)
     * Socket.io 이벤트 전송

[coordinationController.js - 217줄]
- 방 CRUD 작업 재export
- 로그 조회/초기화

[roomController.js - 508줄]
주요 함수:
1. createRoom() - 방 생성, invite code 생성
2. joinRoom() - 방 참가 + 자동배정 트리거
   - 위치: 라인 157-400
   - 자동배정 트리거 조건:
     * 방장이 한 번이라도 자동배정 실행 (timeSlots 존재)
     * 모든 멤버가 선호시간표 설정
     * 방장도 선호시간표 설정
   - 트리거 시 동작:
     * 기존 슬롯에서 시작 날짜 계산
     * 자동배정 실행
     * 새 멤버 포함하여 재배정

2.3 모델
-------
[Room 모델 - 365줄]
주요 스키마:

TimeSlotSchema (라인 24-69):
  - day: String (enum: monday-friday)
  - date: Date (필수)
  - startTime: String (HH:MM)
  - endTime: String (HH:MM)
  - subject: String
  - user: ObjectId (User 참조)
  - status: String (confirmed/pending/conflict)
  - assignedBy: ObjectId (자동배정자)
  - assignedAt: Date
  - priority: Number (1-5)

RequestSchema (라인 71-150):
  - requester: ObjectId
  - type: String (time_request, time_change, slot_swap, etc.)
  - timeSlot: TimeSlotSchema
  - targetSlot: TimeSlotSchema
  - targetUser: ObjectId
  - chainData: Object (연쇄 교환용)
  - status: String (pending/approved/rejected)

RoomSchema (라인 152-312):
  - name: String (필수)
  - owner: ObjectId (필수)
  - members: Array
    * user: ObjectId
    * joinedAt: Date
    * color: String
    * carryOver: Number (이월시간)
    * carryOverHistory: Array
    * totalProgressTime: Number
    * priority: Number (1-5)
  - inviteCode: String (unique)
  - maxMembers: Number (기본 100)
  - timeSlots: [TimeSlotSchema]
  - requests: [RequestSchema]
  - settings:
    * startHour: Number (기본 9)
    * endHour: Number (기본 18)
    * blockedTimes: Array (점심시간 등)
    * roomExceptions: Array (방 예외시간)
    * assignmentMode: String (normal/first_come_first_served/from_today)
    * minHoursPerWeek: Number (기본 3, 범위 0.167-10)
  - logsClearedAt: Object (owner/admin)
  - memberLogsClearedAt: Object
  - autoConfirmAt: Date (자동 확정 예정 시간)

메서드:
  - isOwner(userId): 방장 확인
  - isMember(userId): 멤버 확인
  - getUserColor(userId): 멤버 색상 조회

[User 모델 - 480줄]
주요 필드:

defaultSchedule (라인 227-233):
  - dayOfWeek: Number (0-6, 일요일=0)
  - startTime: String (HH:MM)
  - endTime: String (HH:MM)
  - priority: Number (1-3)
  - specificDate: String (YYYY-MM-DD, 옵션)

scheduleExceptions (라인 236-244):
  - title: String
  - startTime: Date (ISO datetime)
  - endTime: Date (ISO datetime)
  - priority: Number (1-3)
  - specificDate: String (YYYY-MM-DD)
  - isHoliday: Boolean
  - isAllDay: Boolean

personalTimes (라인 247-273):
  - id: Number (고유 ID)
  - title: String
  - type: String (sleep/meal/commute/study/break/custom/event)
  - startTime: String (HH:MM)
  - endTime: String (HH:MM)
  - days: [Number] (1-7, 월요일=1)
  - isRecurring: Boolean
  - specificDate: String (YYYY-MM-DD)
  - color: String

deletedPreferencesByRoom (라인 276-286):
  - roomId: ObjectId
  - deletedTimes: Array (백업된 defaultSchedule 항목)
  - deletedAt: Date

주소 관련 (라인 72-87):
  - address: String
  - addressLat: Number (위도)
  - addressLng: Number (경도)
  - addressPlaceId: String

2.4 크론잡/스케줄러
-----------------
[autoConfirmSchedule.js - 525줄]

1. confirmRoomSchedule(room)
   - 위치: 라인 9-478
   - 역할: confirmSchedule과 동일한 로직 (자동 실행용)
   - 처리:
     * 자동배정 슬롯 병합
     * personalTimes 추가
     * defaultSchedule 분할
     * deletedPreferencesByRoom 백업
     * VersionError 재시도 (최대 3회)

2. processAutoConfirm()
   - 위치: 라인 480-509
   - 역할: 자동 확정이 필요한 방 찾기
   - 쿼리: autoConfirmAt이 null이 아니고 현재 시간보다 이전

3. startAutoConfirmJob()
   - 위치: 라인 511-522
   - 역할: Cron Job 시작
   - 스케줄: */1 * * * * (매 1분마다)
   - 로그: "자동 확정 Cron Job이 시작되었습니다."

2.5 라우트
---------
[coordination.js - 102줄]

자동배정 관련 엔드포인트:
1. POST /api/coordination/rooms/:roomId/run-schedule
   → coordinationController.runAutoSchedule

2. POST /api/coordination/rooms/:roomId/confirm-schedule
   → coordinationSchedulingController.confirmSchedule

3. DELETE /api/coordination/rooms/:roomId/time-slots
   → coordinationController.deleteAllTimeSlots

4. POST /api/coordination/rooms/:roomId/find-common-slots
   → coordinationController.findCommonSlots (AI 기반 분석)

기타 관련:
- POST /rooms - 방 생성
- POST /rooms/:inviteCode/join - 방 참가 (자동배정 트리거 가능)
- GET /rooms/:roomId - 방 조회
- PUT /rooms/:roomId - 방 수정
- DELETE /rooms/:roomId/members/:memberId - 멤버 제거

================================================================================
3. 클라이언트 측 조사
================================================================================

3.1 자동배정 트리거 컴포넌트
--------------------------
[AutoSchedulerPanel.js]
- 위치: client/src/components/scheduler/
- 역할: 자동배정 실행 UI
- 기능:
  * 주당 시간 설정 (0.167-10시간)
  * 배정 주 수 선택 (1-52주)
  * 배정 모드 선택 (normal/first_come_first_served/from_today)
  * 이동수단 선택 (normal/public/driving/walking)
  * "자동 배정 실행" 버튼
- API 호출:
  POST /api/coordination/rooms/:roomId/run-schedule
  Body: { minHoursPerWeek, numWeeks, currentWeek, assignmentMode, transportMode }

[TravelModeButtons.js]
- 위치: client/src/components/coordination/
- 역할: 이동수단 선택 UI
- 옵션:
  * 시간 우선 (normal) - 기본값
  * 대중교통 (public) - Google Maps API 사용
  * 자가용 (driving)
  * 도보 (walking)

[CoordinationTab/index.js]
- 위치: client/src/components/tabs/CoordinationTab/
- 역할: 조정 탭 메인 컴포넌트
- 포함:
  * AutoSchedulerPanel
  * TimetableView (결과 표시)
  * ScheduleAlerts (미배정 멤버 알림)
  * RequestSection (교환 요청)

3.2 결과 표시
------------
[TimetableView]
- 역할: 배정 결과를 시간표 형태로 표시
- 표시 정보:
  * 요일별 슬롯
  * 멤버별 색상 구분
  * 시간 범위
  * 배정 상태 (confirmed/pending)

[ScheduleAlerts.js]
- 역할: 미배정 멤버 알림
- 표시:
  * unassignedMembersInfo 기반
  * 부족한 시간
  * 이월 시간 (carryOver)

[AutoConfirmBanner.js]
- 위치: client/src/components/coordination/
- 역할: 자동 확정 안내 배너
- 기능:
  * autoConfirmAt 카운트다운
  * 수동 확정 버튼
  * 자동 확정 취소 버튼

[ConfirmScheduleModal.js]
- 역할: 수동 확정 모달
- 기능:
  * 확정 전 미리보기
  * 개인일정 추가 확인
  * 선호시간 변경 확인
- API 호출:
  POST /api/coordination/rooms/:roomId/confirm-schedule

3.3 UI 흐름
----------
1. 사용자가 CoordinationTab 진입
2. AutoSchedulerPanel에서 설정
   - 주당 시간
   - 배정 주 수
   - 배정 모드
   - 이동수단
3. "자동 배정 실행" 클릭
4. 서버에서 자동배정 실행
5. 결과 수신 및 TimetableView 렌더링
6. ScheduleAlerts로 미배정 멤버 표시
7. AutoConfirmBanner 표시 (1분 카운트다운)
8. 사용자 액션:
   - 수동 확정: ConfirmScheduleModal 표시 → 확정
   - 자동 확정: 1분 후 크론잡이 자동 실행
   - 취소: 배정 결과 삭제

================================================================================
4. 데이터 흐름 추적
================================================================================

4.1 입력 데이터
-------------
[사용자 입력]
1. 선호시간 (defaultSchedule):
   - 입력 위치: ProfileTab → PreferenceTimeSection
   - 저장: User.defaultSchedule
   - 형식: { dayOfWeek, startTime, endTime, priority, specificDate? }

2. 예외 일정 (scheduleExceptions):
   - 입력: 챗봇 또는 OCR
   - 저장: User.scheduleExceptions
   - 형식: { title, startTime (Date), endTime (Date), priority, specificDate }

3. 개인시간 (personalTimes):
   - 입력: PersonalTimeManager
   - 저장: User.personalTimes
   - 형식: { id, title, type, startTime, endTime, days, isRecurring, specificDate, color }

4. 주소:
   - 입력: ProfileTab → ProfileHeader
   - 저장: User.address, addressLat, addressLng
   - 용도: 대중교통 모드에서 이동시간 계산

[방 설정]
1. 시간 범위:
   - Room.settings.startHour (기본 9)
   - Room.settings.endHour (기본 18)

2. 금지 시간:
   - Room.settings.blockedTimes (점심시간 등)
   - Room.settings.roomExceptions (예외 일정)

3. 배정 설정:
   - Room.settings.minHoursPerWeek (기본 3)
   - Room.settings.assignmentMode

4.2 중간 처리
------------
[1단계: 타임테이블 생성]
서비스: timetableCreationService.js → createTimetableFromPersonalSchedules()

입력:
  - members: 멤버 배열
  - owner: 방장 객체
  - startDate: 시작 날짜
  - numWeeks: 주 수
  - roomSettings: 방 설정

처리 순서:
1. 방장의 가능한 시간대 수집
   - defaultSchedule 기반
   - scheduleExceptions 포함
   - personalTimes 제외
   - blockedTimes 제외

2. 조원들의 개인 시간표 추가
   - defaultSchedule (주간 반복)
   - scheduleExceptions (특정 날짜)
   - personalTimes 제외 (수면, 식사 등)

3. 타임테이블 구조 생성
   ```javascript
   {
     "2025-09-16-09:00": {
       assignedTo: null,
       available: [
         { memberId: "user1", priority: 3, isOwner: false },
         { memberId: "user2", priority: 2, isOwner: false }
       ],
       date: Date("2025-09-16"),
       dayOfWeek: 1  // 1=월요일
     },
     "2025-09-16-09:30": { ... },
     ...
   }
   ```

출력:
  - timetable: 슬롯 키 → 슬롯 데이터 맵

[2단계: 충돌 식별]
서비스: conflictIdentificationService.js → identifyConflictsBeforeAssignment()

입력:
  - timetable
  - ownerId
  - memberRequiredSlots

처리:
1. 각 슬롯의 available 멤버 수 확인
2. 2명 이상이면 충돌로 분류
3. 멤버별 가용 슬롯 수 계산

출력:
  - conflicts: 충돌 슬롯 배열
  - memberAvailableSlots: 멤버별 가용 슬롯 수

[3단계: 슬롯 배정]
서비스: slotAssignmentService.js → assignByTimeOrder() 또는
       publicTransportAssignmentService.js → assignByPublicTransport()

[일반 모드 - assignByTimeOrder()]
처리 순서:
1. 멤버별 가용 슬롯 수 계산
2. 배정 모드에 따라 정렬:
   - normal: 가용 슬롯 적은 순 (선택지 적은 사람 우선)
   - first_come_first_served: joinedAt 빠른 순 (선착순)
   - from_today: 가용 슬롯 적은 순 (오늘 이후만)
3. 각 멤버에 대해:
   - 연속된 블록 찾기 (1시간 단위)
   - 필요량 충족 블록 우선
   - 긴 블록 우선
   - 이른 시간 우선
4. 배정 후 부족분 추가 배정

[대중교통 모드 - assignByPublicTransport()]
처리 순서:
1. 방장 위치를 시작점으로 설정
2. 현재 위치에서 가장 가까운 멤버 찾기:
   - Google Maps Distance Matrix API 호출
   - 이동시간 계산
   - 이동시간 + 수업시간이 선호시간 내 포함 확인
3. 배정 후 현재 위치를 해당 멤버 위치로 업데이트
4. 반복

[4단계: 캐리오버 처리]
서비스: index.js → _carryOverAssignments()

처리:
1. carryOver > 0인 멤버 찾기
2. 빈 슬롯에서 우선 배정
3. 2주 연속 미배정 시 needsIntervention 플래그 설정

4.3 출력 데이터
-------------
[assignments 구조]
```javascript
{
  "userId1": {
    memberId: "userId1",
    assignedHours: 6,  // 슬롯 수 (30분 단위)
    requiredSlots: 6,
    slots: [
      {
        date: Date("2025-09-16"),
        day: "monday",
        startTime: "09:00",
        endTime: "10:00",
        dayOfWeek: 1,
        subject: "자동 배정",
        status: "confirmed"
      },
      ...
    ]
  },
  "userId2": { ... }
}
```

[carryOverAssignments 구조]
```javascript
[
  {
    memberId: "userId3",
    neededHours: 2,  // 슬롯 수
    priority: 3,
    week: Date("2025-09-16")
  }
]
```

[unassignedMembersInfo 구조]
```javascript
[
  {
    memberId: "userId3",
    memberName: "홍길동",
    assignedHours: 4,
    requiredHours: 6,
    shortage: 2
  }
]
```

[Room.timeSlots 저장 형태]
```javascript
[
  {
    user: ObjectId("userId1"),
    date: Date("2025-09-16"),
    startTime: "09:00",
    endTime: "10:00",
    day: "monday",
    priority: 3,
    subject: "자동 배정",
    assignedBy: ObjectId("ownerId"),
    assignedAt: Date.now(),
    status: "confirmed"
  },
  ...
]
```

================================================================================
5. 외부 의존성
================================================================================

5.1 Google Maps API
-------------------
[Distance Matrix API]
- 사용 위치: assignmentHelper.js → calculateTravelTime()
- 용도: 멤버 간 이동시간 계산
- 파라미터:
  * origins: 출발지 (lat,lng 또는 주소)
  * destinations: 목적지 (lat,lng 또는 주소)
  * mode: transit/driving/walking
  * language: ko
- 응답:
  * duration.value: 이동시간 (초)
  * distance.value: 거리 (미터)
- API 키:
  * 환경변수: GOOGLE_MAPS_API_KEY
  * 설정 위치: .env 파일
- 비용:
  * 무료 할당량: 월 $200 크레딧 (약 40,000 요청)
  * 초과 시: $5 per 1,000 requests
- 제한사항:
  * 초당 요청 수 제한
  * 일일 요청 수 제한
- 에러 처리:
  * API 키 없음: 기본값 60분 반환
  * 경로 찾기 실패: 기본값 60분 반환
  * 네트워크 오류: 기본값 60분 반환

5.2 Gemini API
-------------
[사용 위치]
1. coordinationController.js (라인 29)
2. coordinationExchangeController/services/geminiService.js
3. 기타 AI 분석 기능

[용도]
- 충돌 해결 제안
- 교환 요청 파싱
- 일정 최적화 제안

[API 키]
- 환경변수: GEMINI_API_KEY
- 설정 위치: .env 파일

5.3 Socket.IO
------------
[사용 위치]
1. coordinationSchedulingController.js (라인 1002-1008)
2. autoConfirmSchedule.js (라인 462-470)

[이벤트 종류]
1. 'schedule-confirmed'
   - 발생: 수동/자동 확정 후
   - 페이로드:
     ```javascript
     {
       roomId: "roomId",
       message: "자동배정 시간이 확정되었습니다.",
       timestamp: Date.now()
     }
     ```
   - 수신자: room-${roomId} 채널 구독자

2. 기타 이벤트 (추정):
   - 'room-updated': 방 정보 변경
   - 'member-joined': 멤버 참가
   - 'request-created': 교환 요청 생성

[연결 관리]
- 전역 객체: global.io
- 설정: index.js에서 초기화

5.4 데이터베이스 (MongoDB)
-------------------------
[연결]
- URI: process.env.MONGO_URI
- 설정: config/db.js

[주요 쿼리]
1. Room 조회:
   ```javascript
   await Room.findById(roomId)
     .populate('owner', 'firstName lastName email defaultSchedule scheduleExceptions personalTimes address addressLat addressLng')
     .populate('members.user', 'firstName lastName email defaultSchedule address addressLat addressLng')
   ```

2. 자동 확정 대상 조회:
   ```javascript
   await Room.find({
     autoConfirmAt: { $ne: null, $lt: new Date() }
   })
   ```

[인덱싱]
- Room:
  * _id (기본)
  * inviteCode (unique)
  * owner (복합 인덱스 권장: owner + createdAt)
  * autoConfirmAt (크론잡 성능 향상용)

- User:
  * _id (기본)
  * firebaseUid (unique, sparse)
  * email (unique)

[성능 고려사항]
- populate 과다 사용 주의 (N+1 문제)
- autoConfirmAt 인덱스 추가 권장
- 대용량 방 처리 시 페이지네이션 필요

================================================================================
6. 숨겨진 로직 및 비활성화된 기능
================================================================================

6.1 환경 변수
------------
[.env 파일 사용 변수]
1. MONGO_URI - MongoDB 연결 문자열 (필수)
2. GOOGLE_MAPS_API_KEY - Google Maps API 키
3. GEMINI_API_KEY - Gemini AI API 키
4. PORT - 서버 포트 (기본값: 5000)
5. CORS_ORIGIN - CORS 허용 origin (콤마 구분)
6. NODE_ENV - 환경 (production/development)

[확인된 위치]
- index.js (라인 9): dotenv.config()
- index.js (라인 20-23): MONGO_URI 검증
- assignmentHelper.js (라인 203): GOOGLE_MAPS_API_KEY
- 여러 컨트롤러: GEMINI_API_KEY

6.2 주석 처리된 코드
------------------
[index.js - Phase 2, 3]
위치: 라인 174-191

주석 처리된 배정 단계:
```javascript
// Phase 2: 논쟁 없는 슬롯 배정 (고우선순위)
// assignUndisputedSlots(timetable, assignments, 3, memberRequiredSlots, conflictingSlots);

// Phase 2-2: 논쟁 없는 슬롯 배정 (저우선순위)
// assignUndisputedSlots(timetable, assignments, 1, memberRequiredSlots, conflictingSlots);

// Phase 3: 반복적 배정
// iterativeAssignment(
//   timetable,
//   assignments,
//   2,
//   memberRequiredSlots,
//   nonOwnerMembers,
//   ownerPreferences,
//   conflictingSlots,
//   ownerId
// );
```

이유:
- "시간 순서 우선 배정 (assignByTimeOrder)" 도입으로 대체
- 기존 방식은 충돌 해결이 복잡했음
- 새 방식이 더 직관적이고 성능 우수

상태: slotAssignmentService.js에 함수 코드는 존재 (라인 338-605)

[index.js - Phase 4, 5]
위치: 라인 193-197

주석 처리된 충돌 해결:
```javascript
// Phase 4: 방장 슬롯 가져가기 (현재 미사용)
// resolveConflictsByOwnerTakingSlot(timetable, assignments, owner, memberRequiredSlots, ownerPreferences);

// Phase 5: 방장 양보 충돌 해결 (시간 순서 배정 사용으로 비활성화)
// resolveConflictsWithOwner(timetable, assignments, owner, memberRequiredSlots);
```

이유:
- 시간 순서 배정으로 충돌이 사전에 방지됨
- 방장 개입 로직은 복잡도 증가

상태: conflictResolutionService.js에 함수 코드 존재

6.3 Negotiation 기능 (제거됨)
---------------------------
[제거 흔적]
1. index.js (라인 160):
   ```javascript
   // Negotiation blocks feature removed
   ```

2. index.js (라인 219):
   ```javascript
   // Negotiation feature removed
   ```

3. coordinationSchedulingController.js (라인 234):
   ```javascript
   // Negotiations feature removed
   ```

4. roomController.js (라인 422):
   ```javascript
   // Negotiation feature removed
   ```

5. multiWeekSchedulingService.js (라인 75, 85):
   ```javascript
   // Negotiation feature removed
   // Negotiation feature removed - addWeekInfoToNegotiations function deleted
   ```

[원래 기능 (추정)]
- 충돌 슬롯에 대해 멤버 간 협의 제안
- negotiationBlocks 배열 생성
- 우선순위 기반 협의 순서 결정
- 자동 협의 요청 생성

[제거 이유]
- 복잡도 증가
- 사용자 경험 저하 (협의 과정이 번거로움)
- 자동배정의 본래 목적과 상충 (자동화 vs 수동 협의)

[대체 방안]
- 교환 요청 시스템 (coordinationExchangeController)
- 수동 슬롯 이동 (드래그 앤 드롭)

6.4 설정 가능한 파라미터
----------------------
[coordinationSchedulingController.js]
1. autoConfirmDelay (라인 336):
   ```javascript
   const autoConfirmDelay = 1 * 60 * 1000; // 1분 = 60,000ms
   ```
   - 현재: 1분 (실험용)
   - 프로덕션 권장: 48시간 (48 * 60 * 60 * 1000)

[schedulingConstants.js]
1. DEFAULT_REQUIRED_SLOTS: 6 (3시간 * 2슬롯/시간)
2. SLOTS_PER_HOUR: 2 (30분 단위)
3. MINUTES_PER_SLOT: 30
4. MAX_ITERATION_ROUNDS: 100
5. FAIRNESS_GAP_THRESHOLD: 2

[timeConstants.js]
1. DEFAULT_SCHEDULE_START_TIME: "09:00"
2. DEFAULT_SCHEDULE_END_TIME: "18:00"

================================================================================
7. 에러 처리
================================================================================

7.1 예외 상황 처리
----------------
[1. 시간 부족]
발생: 멤버들의 선호시간이 부족하여 필요 시간 미충족

처리:
- carryOverAssignments 생성
- unassignedMembersInfo에 부족 정보 포함
- 클라이언트에서 ScheduleAlerts 표시

코드 위치:
- memberHelper.js → createCarryOverAssignments()
- memberHelper.js → createUnassignedMembersInfo()

[2. 충돌 해결 실패]
발생: 멤버들이 같은 시간대만 선호

처리:
- conflictIdentificationService에서 충돌 식별
- 우선순위 기반 배정
- 낮은 우선순위 멤버는 carryOver

코드 위치:
- conflictIdentificationService.js
- slotAssignmentService.js → sortMembersByMode()

[3. API 오류 (Google Maps)]
발생: Distance Matrix API 호출 실패

처리:
- 기본값 60분 반환
- console.error 로그
- 배정 계속 진행

코드 위치:
- assignmentHelper.js → calculateTravelTime() (라인 201-248)
  ```javascript
  if (!apiKey) {
    console.error('Google Maps API Key가 설정되지 않았습니다.');
    return 60; // 기본값 1시간
  }
  ```

[4. 선호시간표 미설정]
발생: 멤버가 defaultSchedule 미설정

처리:
- 자동배정 실행 전 검증
- 400 에러 반환
- 에러 메시지: "멤버들이 선호시간표를 설정하지 않았습니다"

코드 위치:
- coordinationSchedulingController.js (라인 77-112)
  ```javascript
  const membersWithoutDefaultSchedule = [];
  for (const member of membersOnly) {
    const hasDefaultSchedule = member.user?.defaultSchedule && member.user.defaultSchedule.length > 0;
    const hasScheduleExceptions = member.user?.scheduleExceptions && member.user.scheduleExceptions.length > 0;

    if (!member.user || (!hasDefaultSchedule && !hasScheduleExceptions)) {
      const userName = member.user?.name || `${member.user?.firstName || ''} ${member.user?.lastName || ''}`.trim() || '알 수 없음';
      membersWithoutDefaultSchedule.push(userName);
    }
  }
  if (membersWithoutDefaultSchedule.length > 0) {
    return res.status(400).json({
      msg: `다음 멤버들이 선호시간표를 설정하지 않았습니다: ${membersWithoutDefaultSchedule.join(', ')}`
    });
  }
  ```

[5. VersionError (MongoDB)]
발생: 동시 업데이트로 인한 버전 충돌

처리:
- 재시도 로직 (최대 3회)
- 최신 버전 다시 조회
- 변경사항 재적용
- 100ms * attempt 대기 후 재시도

코드 위치:
- coordinationSchedulingController.js (라인 926-990)
- autoConfirmSchedule.js (라인 382-442)

7.2 로깅
-------
[console.log 종류]

1. 자동배정 실행:
   ```
   🔍 ===== [서버] 자동배정 요청 받음 =====
   📥 받은 파라미터: { minHoursPerWeek, numWeeks, currentWeek, assignmentMode }
   📅 계산된 startDate: 2025-09-16
   🔍 ===================================
   ```

2. 타임테이블 생성:
   ```
   🔍 [타임테이블] 생성: 2025-09-16 ~ 2025-10-14
   ```

3. 배정 진행:
   ```
   🕐 시간 순서 배정 시작 (슬롯: 280개)
   --- 1단계: 선택지 적은 멤버 우선 배정 ---
   📊 멤버 처리 순서 (배정 모드: normal):
      abc123: 우선순위 3, 가용 120슬롯
   📋 [abc123] 필요: 6슬롯, 가용: 120슬롯
   ✅ [배정] abc123... → 2025-09-16 09:00-10:00 (2 슬롯)
   --- 1단계 완료 ---
   ```

4. 대중교통 모드:
   ```
   🚌 ===== 대중교통 모드 배정 시작 =====
   📅 [monday] 배정 시작
   📍 [대중교통 모드] 가까운 순서로 3명 확인
   🚌 이동시간 계산: 25분 (transit)
   ✅ 홍길동 배정 완료: 09:00-10:00 (2슬롯)
   🚌 ===== 대중교통 모드 배정 완료 =====
   ```

5. 최종 결과:
   ```
   📊 ===== [서버] 최종 배정 결과 =====
   총 슬롯 수: 24
   날짜 범위: 2025-09-16 ~ 2025-09-20
   총 배정일 수: 5
   월별 배정일 수: { '2025-09': 5 }
   🔍 ===================================
   ```

6. 자동 확정:
   ```
   🔔 [자동확정] 2개 방의 자동 확정 시작...
   ✅ [자동확정] 방 abc123 (테스트방): 성공적으로 확정됨
   📡 [자동확정] Socket 이벤트 전송: room-abc123
   ✅ [자동확정] 처리 완료
   ```

7. 에러:
   ```
   ⚠️ VersionError for user abc123, retrying (1/3)...
   ❌ [자동확정] 방 abc123 확정 실패: VersionError
   ```

[디버깅 정보]
- 모든 로그는 한글 + 이모지 사용
- 로그 레벨 없음 (모두 console.log)
- 프로덕션 환경에서는 로그 레벨 시스템 필요 (winston 등)

================================================================================
8. 성능 및 최적화
================================================================================

8.1 알고리즘 복잡도
-----------------
[타임테이블 생성]
- 시간 복잡도: O(M * W * D * S)
  * M: 멤버 수
  * W: 주 수
  * D: 일 수 (5일, 월-금)
  * S: 슬롯 수/일 (평균 18슬롯, 09:00-18:00)
- 공간 복잡도: O(W * D * S)
- 예시: 10명, 4주 = O(10 * 4 * 5 * 18) = O(3,600)

[시간 순서 배정]
- 시간 복잡도: O(M * T * log(T))
  * M: 멤버 수
  * T: 총 슬롯 수
  * log(T): 블록 정렬
- 최악: 모든 멤버가 모든 슬롯 사용 가능
- 평균: 멤버당 20-30% 슬롯만 사용 가능

[대중교통 모드]
- 시간 복잡도: O(M^2 * D * A)
  * M: 멤버 수
  * D: 일 수
  * A: API 호출 시간 (평균 200-500ms)
- 예시: 10명, 5일 = 약 50회 API 호출 = 10-25초
- 병목: Google Maps API 응답 시간

[충돌 식별]
- 시간 복잡도: O(T)
  * T: 총 슬롯 수
- 각 슬롯의 available 배열 순회

8.2 병목 지점
------------
[1. 대용량 방 처리]
문제:
- 멤버 100명 이상 시 타임테이블 생성 느림
- populate 과다 사용 (N+1 문제)

해결 방안:
- 멤버 배치 처리 (20명씩)
- populate 최적화 (필요한 필드만 선택)
- 인덱싱 강화

[2. Google Maps API]
문제:
- 순차 호출로 인한 대기 시간
- 무료 할당량 초과 가능

해결 방안:
- 병렬 호출 (Promise.all)
- 캐싱 (같은 출발지-목적지는 재사용)
- 거리 기반 필터링 (너무 먼 멤버 제외)

[3. 다중 주 배정]
문제:
- 52주 배정 시 매우 느림
- 메모리 사용량 증가

해결 방안:
- 최대 주 수 제한 (예: 12주)
- 배치 배정 (4주씩)
- 타임테이블 재사용

[4. VersionError]
문제:
- 동시 업데이트 시 빈번한 재시도

해결 방안:
- 배치 업데이트 최소화
- 트랜잭션 사용 (MongoDB 4.0+)
- 락 메커니즘 도입

8.3 개선 제안
------------
[1. 캐싱]
```javascript
// Redis 캐싱 예시
const Redis = require('ioredis');
const redis = new Redis();

// 이동시간 캐싱
async function calculateTravelTimeWithCache(origin, destination, mode) {
  const cacheKey = `travel:${origin.lat},${origin.lng}:${destination.lat},${destination.lng}:${mode}`;
  const cached = await redis.get(cacheKey);
  if (cached) return parseInt(cached);

  const travelTime = await calculateTravelTime(origin, destination, mode);
  await redis.set(cacheKey, travelTime, 'EX', 3600 * 24); // 24시간 캐시
  return travelTime;
}
```

[2. 인덱싱]
```javascript
// Room 컬렉션 인덱스
db.rooms.createIndex({ owner: 1, createdAt: -1 });
db.rooms.createIndex({ autoConfirmAt: 1 });
db.rooms.createIndex({ 'members.user': 1 });

// User 컬렉션 인덱스
db.users.createIndex({ firebaseUid: 1 }, { unique: true, sparse: true });
db.users.createIndex({ email: 1 }, { unique: true });
```

[3. 로그 레벨]
```javascript
// Winston 로거 도입
const winston = require('winston');
const logger = winston.createLogger({
  level: process.env.LOG_LEVEL || 'info',
  format: winston.format.json(),
  transports: [
    new winston.transports.File({ filename: 'error.log', level: 'error' }),
    new winston.transports.File({ filename: 'combined.log' })
  ]
});

if (process.env.NODE_ENV !== 'production') {
  logger.add(new winston.transports.Console({
    format: winston.format.simple()
  }));
}
```

[4. 배치 처리]
```javascript
// 대용량 멤버 배치 배정
async function runAutoScheduleBatch(members, owner, options, batchSize = 20) {
  const batches = [];
  for (let i = 0; i < members.length; i += batchSize) {
    batches.push(members.slice(i, i + batchSize));
  }

  const results = [];
  for (const batch of batches) {
    const result = await runAutoSchedule(batch, owner, [], options);
    results.push(result);
  }

  return mergeBatchResults(results);
}
```

================================================================================
9. 환경 변수 전체 목록
================================================================================

[.env 파일 필수 변수]
1. MONGO_URI
   - 설명: MongoDB 연결 문자열
   - 형식: mongodb://localhost:27017/ai_schedule 또는 mongodb+srv://...
   - 필수 여부: 필수
   - 검증 위치: index.js (라인 20-23)

2. GOOGLE_MAPS_API_KEY
   - 설명: Google Maps Distance Matrix API 키
   - 용도: 이동시간 계산 (대중교통 모드)
   - 필수 여부: 선택 (없으면 기본값 60분 사용)
   - 사용 위치: assignmentHelper.js (라인 203)

3. GEMINI_API_KEY
   - 설명: Google Gemini AI API 키
   - 용도: 충돌 해결 제안, 일정 분석
   - 필수 여부: 선택 (AI 기능만 영향)
   - 사용 위치: 여러 컨트롤러

4. PORT
   - 설명: 서버 포트
   - 기본값: 5000
   - 필수 여부: 선택
   - 사용 위치: index.js (라인 150)

5. CORS_ORIGIN
   - 설명: CORS 허용 origin (콤마 구분)
   - 예시: http://localhost:3000,http://localhost:3001
   - 기본값: localhost:3000, 127.0.0.1:3000
   - 필수 여부: 선택
   - 사용 위치: index.js (라인 31-37)

6. NODE_ENV
   - 설명: 실행 환경
   - 값: production, development
   - 기본값: development
   - 영향:
     * 로그 레벨
     * CORS 정책
     * 에러 메시지 상세도
   - 사용 위치: index.js (라인 47, 97, 120)

[.env 파일 예시]
```
MONGO_URI=mongodb://localhost:27017/ai_schedule
GOOGLE_MAPS_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
GEMINI_API_KEY=AIzaSyYYYYYYYYYYYYYYYYYYYYYYYYYYYYYY
PORT=5000
CORS_ORIGIN=http://localhost:3000,http://localhost:3001
NODE_ENV=development
```

================================================================================
10. 누락된 부분 및 개선 제안
================================================================================

10.1 발견된 누락 부분
-------------------
[1. 에러 로그 시스템 부재]
현황:
- 모든 로그가 console.log
- 로그 레벨 없음
- 로그 파일 저장 없음
- 프로덕션 환경에서 디버깅 어려움

권장 개선:
- Winston 또는 Bunyan 도입
- 로그 레벨 분리 (error, warn, info, debug)
- 파일 로테이션 (일별, 크기별)
- 에러 추적 (Sentry 등)

[2. API 응답 표준화 부재]
현황:
- 성공 응답 형식 불일치
  * 어떤 곳: { room: {...} }
  * 어떤 곳: { msg: "성공", data: {...} }
- 에러 응답 형식 불일치
  * 어떤 곳: { msg: "에러 메시지" }
  * 어떤 곳: { message: "에러 메시지" }

권장 개선:
```javascript
// 표준 응답 형식
{
  success: true,
  data: { ... },
  message: "성공 메시지",
  timestamp: "2025-12-09T10:00:00Z"
}

// 표준 에러 형식
{
  success: false,
  error: {
    code: "VALIDATION_ERROR",
    message: "에러 메시지",
    details: { ... }
  },
  timestamp: "2025-12-09T10:00:00Z"
}
```

[3. 입력 검증 미흡]
현황:
- 일부 엔드포인트에만 검증 존재
- 검증 로직 중복
- sanitization 부재

권장 개선:
- express-validator 도입
- 공통 검증 미들웨어
- 입력 sanitization

예시:
```javascript
const { body, validationResult } = require('express-validator');

router.post('/rooms/:roomId/run-schedule',
  auth,
  [
    body('minHoursPerWeek').isFloat({ min: 0.167, max: 10 }),
    body('numWeeks').isInt({ min: 1, max: 12 }),
    body('assignmentMode').isIn(['normal', 'first_come_first_served', 'from_today'])
  ],
  (req, res, next) => {
    const errors = validationResult(req);
    if (!errors.isEmpty()) {
      return res.status(400).json({ errors: errors.array() });
    }
    next();
  },
  coordinationSchedulingController.runAutoSchedule
);
```

[4. 테스트 코드 부재]
현황:
- 단위 테스트 없음
- 통합 테스트 없음
- E2E 테스트 없음

권장 개선:
- Jest 도입
- 핵심 알고리즘 단위 테스트
- API 엔드포인트 통합 테스트

예시:
```javascript
// __tests__/schedulingAlgorithm.test.js
describe('SchedulingAlgorithm', () => {
  describe('runAutoSchedule', () => {
    it('should assign all members when enough slots', async () => {
      const members = [/* mock data */];
      const owner = {/* mock data */};
      const result = await schedulingAlgorithm.runAutoSchedule(
        members, owner, [], { minHoursPerWeek: 3, numWeeks: 1 }
      );

      expect(Object.keys(result.assignments).length).toBe(members.length);
      expect(result.carryOverAssignments.length).toBe(0);
    });

    it('should create carryOver when insufficient slots', async () => {
      // ...
    });
  });
});
```

[5. 문서화 부재]
현황:
- API 문서 없음
- JSDoc 주석 일부만 존재
- 함수 파라미터 타입 불명확

권장 개선:
- Swagger/OpenAPI 도입
- JSDoc 표준화
- TypeScript 마이그레이션 고려

[6. 모니터링 부재]
현황:
- 성능 메트릭 수집 없음
- API 응답 시간 모니터링 없음
- 에러율 추적 없음

권장 개선:
- Prometheus + Grafana
- API 응답 시간 측정
- 에러율 알림

[7. 보안 강화 필요]
현황:
- API rate limiting 없음
- SQL injection 방어 (MongoDB는 기본 방어)
- XSS 방어 부분적

권장 개선:
```javascript
const rateLimit = require('express-rate-limit');

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15분
  max: 100 // 최대 100 요청
});

app.use('/api/', limiter);
```

10.2 알고리즘 개선 제안
--------------------
[1. 머신러닝 기반 배정]
현황: 규칙 기반 배정
제안: 과거 데이터 학습하여 최적 배정 예측

[2. 멀티 스레드 처리]
현황: 단일 스레드
제안: Worker Threads로 병렬 처리

[3. 실시간 재배정]
현황: 전체 재배정만 가능
제안: 일부 멤버만 재배정 (증분 배정)

[4. 충돌 자동 해결]
현황: carryOver로 연기
제안: AI 기반 대안 시간 제안

10.3 데이터베이스 최적화
----------------------
[1. 인덱싱 추가]
```javascript
// Room
db.rooms.createIndex({ owner: 1, createdAt: -1 });
db.rooms.createIndex({ autoConfirmAt: 1 }, { sparse: true });
db.rooms.createIndex({ 'members.user': 1 });
db.rooms.createIndex({ inviteCode: 1 }, { unique: true });

// User
db.users.createIndex({ firebaseUid: 1 }, { unique: true, sparse: true });
db.users.createIndex({ email: 1 }, { unique: true });
db.users.createIndex({ 'defaultSchedule.dayOfWeek': 1 });

// ActivityLog
db.activityLogs.createIndex({ roomId: 1, createdAt: -1 });
db.activityLogs.createIndex({ userId: 1, createdAt: -1 });
```

[2. 쿼리 최적화]
```javascript
// 나쁜 예
const room = await Room.findById(roomId).populate('members.user');
for (const member of room.members) {
  const user = await User.findById(member.user._id);
  // N+1 문제
}

// 좋은 예
const room = await Room.findById(roomId)
  .populate({
    path: 'members.user',
    select: 'firstName lastName email defaultSchedule'
  });
```

[3. 트랜잭션 사용]
```javascript
const session = await mongoose.startSession();
session.startTransaction();

try {
  // 여러 업데이트
  await room.save({ session });
  await user.save({ session });

  await session.commitTransaction();
} catch (error) {
  await session.abortTransaction();
  throw error;
} finally {
  session.endSession();
}
```

10.4 클라이언트 개선 제안
-----------------------
[1. 실시간 진행 표시]
현황: 자동배정 중 로딩만 표시
제안: Socket.io로 진행률 전송

[2. 배정 결과 시각화]
현황: 단순 테이블 표시
제안: 통계, 차트, 히트맵

[3. 오프라인 모드]
현황: 항상 온라인 필요
제안: IndexedDB로 로컬 캐싱

================================================================================
최종 요약
================================================================================

[시스템 현황]
✅ 완성도: 85%
✅ 핵심 기능: 완전 구현
✅ 코드 품질: 양호 (모듈화, 주석)
⚠️ 테스트: 부재
⚠️ 문서화: 부족
⚠️ 모니터링: 부재

[주요 강점]
1. 모듈화된 구조 (24개 파일, 4,517줄)
2. 명확한 책임 분리 (서비스/헬퍼/유틸)
3. 다양한 배정 전략 (시간 순서, 대중교통, 선착순)
4. 실시간 업데이트 (Socket.IO)
5. 자동 확정 시스템 (Cron Job)
6. 이월시간 관리
7. 선호시간 백업/복구

[개선 필요 항목]
1. 테스트 코드 작성 (우선순위: 높음)
2. API 문서화 (우선순위: 높음)
3. 에러 로그 시스템 (우선순위: 높음)
4. 성능 모니터링 (우선순위: 중간)
5. 인덱싱 최적화 (우선순위: 중간)
6. 캐싱 시스템 (우선순위: 낮음)

[즉시 조치 권장]
1. autoConfirmDelay를 48시간으로 변경
2. GOOGLE_MAPS_API_KEY 할당량 모니터링
3. VersionError 로그 분석 및 트랜잭션 도입 검토
4. autoConfirmAt 인덱스 추가

[장기 개선 로드맵]
Phase 1 (1-2개월):
  - Jest 단위 테스트 작성
  - Swagger API 문서화
  - Winston 로그 시스템 도입
  - 인덱싱 최적화

Phase 2 (3-4개월):
  - Prometheus 모니터링
  - Redis 캐싱
  - 트랜잭션 도입
  - E2E 테스트

Phase 3 (5-6개월):
  - TypeScript 마이그레이션
  - 머신러닝 배정
  - 성능 최적화
  - 보안 강화

[결론]
자동배정 알고리즘은 핵심 기능이 완전히 구현되어 있으며, 모듈화가 잘 되어 있습니다.
다만 프로덕션 환경에서 안정적으로 운영하기 위해서는 테스트, 모니터링, 문서화가
필요합니다. 위의 개선 사항들을 단계적으로 적용하면 엔터프라이즈급 시스템으로
발전할 수 있습니다.

================================================================================
작성자: Claude Sonnet 4.5
작성일: 2025-12-09
버전: 1.0
================================================================================
